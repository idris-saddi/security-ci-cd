{
  "name": "devsecops-app",
  "version": "1.0.0",
  "dependencies": {
    "express": "^4.21.1",
    "express-rate-limit": "^7.2.0",
    "helmet": "^7.1.0"
  },
  "scripts": {
    "start": "node server.js",
    "test": "echo 'No unit tests configured yet' && exit 0"
  }
}